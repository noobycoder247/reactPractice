// We are creating this because we import all the Component here and in other files where we want to use that components we just have to import from single file
import InputBox from "./InputBox"

export {InputBox}